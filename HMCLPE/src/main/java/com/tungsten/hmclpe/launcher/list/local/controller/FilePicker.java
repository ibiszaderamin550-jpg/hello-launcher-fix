package com.tungsten.hmclpe.launcher.list.local.controller;

import android.app.Activity;
import android.content.Intent;

public class FilePicker {
    // Gradle kompilyasiyadan keçsin deyə saxta (stub) class
    public static class Builder {
        public Builder(Activity activity) { }
        public Builder setActivity(Activity activity) { return this; }
        public Intent build() { return new Intent(); }
    }
}
